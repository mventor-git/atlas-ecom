# Atlas Ecom Project State

**As of:** 2026-09-26
**Phase:** Contract at **v1.3.0, dated 2026-09-26, and published.** The v1.2.0 amendment is published as `a954e3d` and the frontend it approves as `a2edd7d`; v1.3.0 shipped on top of that as `f5fc6fa` ("docs: add legacy transfer and token parity", 6 files, 812 insertions / 21 deletions), carrying `contract.md`, `docs/LEGACY_KNOWLEDGE_TRANSFER.md`, `scripts/check-token-parity.mjs`, the root `package.json` `tokens:parity` wiring, the `.gitignore` `.vitest/` rule, and the previous revision of this file. One commit sits on top of it, `a1bc6a4` ("fix: cover success text tokens in parity check", 1 file, 6 insertions), which widened the parity list to the two `state.success` variables. `HEAD` and `origin/main` both resolve to `a1bc6a4` with no ahead/behind count, and the only working-tree change left in this repository is this file's own revision. `contract.md` v1.3.0 adds five things: the **legacy knowledge transfer** rule, an **evolution map** of candidate capabilities, the **cross-product ownership** rule, a **token-parity** acceptance requirement, and **acceptance gate 8** ("legacy transfer"). None of them adds a capability, a product boundary, a cluster, a dependency, or approved scope. `frontend/` is a separate Vite 8 + React 19 + TypeScript + Tailwind CSS v4 application with nine shadcn components generated into `src/components/ui`, one token bridge, and two routes in one bundle — the customer storefront at `/` and the `ecom-manager` at `/manager`. Both routes have now been driven in a real browser against a live API in light and dark mode, which is the one verification every number in this file previously lacked. It reads the existing `node:http` JSON API and nothing else: the dev and preview servers proxy `/api` to `ATLAS_ECOM_API_ORIGIN` (default `http://127.0.0.1:4312`), there is no fixture fallback and no duplicated commerce logic, and every price is the integer cents the API sent. The one new piece of API surface it needed is `POST /api/catalog`, so the manager's Add Product is a JSON write with the server owning the catalogue instead of a native form post. Both server-rendered `node:http` pages are still there and still serve; nothing was migrated, replaced, or removed. **The whole v1.2.0 implementation is published**: `a2edd7d` carries all 41 `frontend/` files, the `POST /api/catalog` change in `src/web-server.ts`, its tests in `test/web-server.test.ts`, and this file, and the state file itself is published as `3bd58d7`. **The v1.3.0 work on top of it is published too**: `f5fc6fa` and `a1bc6a4` between them carry the contract amendment, both new files, the `tokens:parity` wiring, and the `.gitignore` rule, and the branch is neither ahead of nor behind `origin/main`. What gate 7 actually gets is narrower than "met", and this file does not claim more: the approved direction, the shadcn-to-Atlas mapping, the generated components, light/dark with a persisted choice, and a real production build are demonstrated on the demo surface, against fixture data behind a live in-memory API, with no authentication, no persistence, no payment, and no ERP connection, and the older gates that were already partial are unchanged by it. The browser pass now confirms the surfaces *run*, which was never in doubt and was worth proving anyway; it confirms nothing about the data underneath them. The product itself is where the previous phase left it: partial standalone commerce slice with a durable connected checkout. `StandaloneCatalog` + `Cart` cover catalog -> cart -> local checkout -> a returned immutable `Order`, `OrderBook` provides in-memory readback, and `AtlasErpClient` reads ERP health, manifest, and audit data. `ConnectedCheckout` builds a real Ecom order from `ConnectedCatalog` stock, posts it to ERP, and keeps the answer in a real PostgreSQL `connected_orders` table, so a retried checkout replays the stored order instead of posting a second sale. A reproducible example database (`db/`) ships a fictional six-product catalogue in an `ecom_example` schema; it is not production persistence and no application code reads it. `src/web-server.ts` serves the first browser surface on top of that seed, on loopback, all process-local and connected to no peer, and both of its pages render entirely from the shared Atlas UI design tokens in `contract.md` v1.1.1 with an explicit `?theme=light|dark` mode carried across them. The tokenization shipped painting `fg.muted` on `bg.surface`, which is 3.48:1 in dark mode and below AA, so the stylesheet now keeps the muted token on the canvas it was derived against and paints `fg.default` on every secondary line a card, a table, a stat tile, a buy row or a disabled control draws, and a contrast suite measures the painted pairs in both modes. Two pairs still fall short of the contract's AA target and are recorded as shortfalls in the suite rather than hidden: the light `state.success` pair at 3.38:1, which the contract itself keeps as supplied, and dark `border.control` on `bg.surface` at 2.79:1, which the token table cannot reach. No acceptance gate is complete; the product is still in progress.

## Implemented

- TypeScript registry for `core -> cluster -> plugin -> module`, with the local commerce hierarchy and the catalog, orders, presentation, cart, checkout, and payment capability seeds. The durable-checkout slice added the first third-party runtime dependency, `pg` (^8.23.0), used only by the durable order store; the tokenization added none, and every other path still runs on the standard library.
- Process-local Atlas Connect kernel covering major-version handshake, malformed-manifest rejection, share-only pairing, explicit `master`/`reader`/`proposer` authority, snapshots with opaque cursors, ordered deltas, idempotent inbox handling, and proposal transitions.
- `npm run smoke` standalone registry entry point. The protocol adapter is deliberately volatile test infrastructure, not production persistence.
- In-memory `StandaloneCatalog` for local product add/update/deactivate/list and `Cart` for active-product lines, availability checks, integer-cent totals, and first-added line order. Both operate without a peer.
- `Cart.checkout()` revalidates active products, current prices, and availability; rejects empty, stale-price, inactive, insufficient-stock, and duplicate-line inputs before mutation; atomically decrements catalog quantities; clears the cart; and returns an immutable `Order` with `status: "created"` and frozen line/total snapshots. This local checkout path is unchanged and its returned order is still not persisted.
- `OrderBook` records successful checkouts in memory and exposes `getOrder`, `listOrders`, and `summary`; it returns frozen order copies, omits unknown IDs, and does not take ownership of ERP data. `Order` gained three optional fields for peer-backed orders: `status` also accepts `"submitted"`, plus `source: "connected"` and the peer's own `remote_sale_id` so the two records can be reconciled without either side owning the other's data.
- `OrderBook.recordConnectedOrder` is the single controlled write path for a peer-backed order. It rejects anything that is not a `submitted` `connected` order, requires a non-empty `remote_sale_id`, and refuses to replace an id that is already recorded, so an order cannot be rewritten by a second attempt.
- `AtlasErpClient` is a minimal real HTTP client for the ERP loopback profile: health is read without authentication, manifest, audit, and sale-post reads/writes use the configured bearer token, requests are bounded, and response shapes are validated without leaking the token in errors. The audit payload is fully typed (`ErpAuditItem`, `ErpStockLevel`, `ErpSale`, `ErpJournalEntry`, `ErpStockMovement`, `ErpAuditData`, `ErpAuditSnapshot`), including the ERP item master.
- `ErpAuditConsumer` is the read side of the audit capability. It pins the peer `app_id`/`capability` and rejects an identity change, keeps only the last applied opaque cursor so re-reading the same position is a no-op, and exposes `listItems`, `listStock`, and `getStockLevel`. It projects ERP records; it does not adopt or own them. State is process-local, so a restart re-reads from the peer rather than resurrecting stale data, and there is no delta chain to walk in this snapshot-only profile.
- `ConnectedCatalog` is a read-only storefront projection joining ERP items to current stock into `ConnectedCatalogEntry` (`item_id`, `sku`, `name`, `price_cents`, `quantity`), with `list`, `getById`, and `getBySku`. An item with no stock record joins as quantity 0, which reads as unavailable. It holds no `StandaloneCatalog` and exposes no write path.
- `src/postgres-order-store.ts` is Ecom's durable record of a submitted connected order command. One `OrderCommandStore` interface declares `ensureSchema` / `reserve` / `complete` / `abort` / `close`, with two adapters: `InMemoryOrderCommandStore` for tests and standalone use, and `PostgresOrderCommandStore` for durability. The durable adapter owns exactly one Ecom table, `connected_orders` (`order_id` primary key, `request_hash`, a `status` check limited to `pending`/`completed`, a `response` jsonb receipt, `created_at`, `updated_at`). `request_hash` is the SHA-256 of the exact payload submitted, so the same key with a different payload is a conflict rather than a silent second order. `reserve` inserts with `ON CONFLICT DO NOTHING` inside one transaction so concurrent claims of one id resolve to exactly one winner. The table name is a constant, so no caller-supplied value reaches the SQL text.
- `ConnectedCheckout` is a real connected checkout rather than a smoke: one Ecom order, one peer submission, one durable receipt. It refuses to be constructed without `allowWrites: true`, resolves the item from `ConnectedCatalog`, rejects overselling and unsellable prices, reserves the caller's key, posts one single-line sale, verifies the peer's `total_cents` equals `unit_price_cents * quantity`, and only then completes the receipt. The peer's `sale_id` is derived from the key as `ecom-${key}`, so even a released claim that is retried cannot become a second peer sale. A failed peer call, or a peer total that does not match, aborts the receipt so the key can be retried; a failed abort is swallowed deliberately, because a claim left pending is the safe direction. A replayed receipt is re-validated field by field rather than trusted, so a hand-edited row fails loudly instead of handing back a malformed order, and recording is idempotent (`getOrder` before `recordConnectedOrder`) so a replay never replaces the first order.
- `connect:order-smoke` and `createManualSale` remain as the lower-level path: one typed manual-sale request and a guarded single-unit sale for the first in-stock item, still behind `ATLAS_ERP_ALLOW_WRITE=1`. They are a smoke, not a checkout, and they build no Ecom `Order`.
- `connect:checkout-smoke` is the real connected checkout against a real peer and a real database. It refuses to run without `ATLAS_ERP_ALLOW_WRITE=1`, reads its whole configuration before opening a peer connection or a database session, and never prints the connection string. It sells one unit, re-reads the peer audit and requires the cursor to advance and stock to fall by exactly the submitted quantity, then closes the order store, opens a new one, and repeats the same key: the replay must be answered from the stored receipt with the same order id and `sale_id`, must *not* move the peer's audit cursor, must leave stock unchanged, and must leave exactly one connected order in the `OrderBook`. Any of those failing is a non-zero exit.
- `db/` is a reproducible example dataset, not production persistence and not a migration set. `db/schema.sql` creates five tables in a dedicated `ecom_example` schema and `db/seed.sql` loads the shared catalogue: 6 categories, 6 products, 37 colour/size variants, exactly one image row per variant, and 37 stock rows. It lives in its own schema so it can sit beside real Ecom tables and be removed with one `DROP SCHEMA ecom_example CASCADE`. `products.product_id` deliberately holds the same value as the ERP item master `item_id`, and `variant_id` is the deterministic `<PRODUCT SKU>-<COLOR>-<SIZE>` composite, so this dataset and the `atlas-erp` example seed can be diffed or joined by id. Nothing in `src/` reads these tables: the only table the application owns is still `connected_orders`. Two seeded edge cases are deliberate: the two `Ember` bottle variants are `inactive`, and `SOL-TRAIL-BTL-EMBER-750ML` has 1 unit on hand so an oversell can be rejected.
- `src/demo-catalog.ts` is the UI's fixture: the `db/seed.sql` example catalogue as in-process data, copied verbatim where identity, price, stock, status, and imagery matter (6 products, 37 colour/size variants, one Unsplash image per variant, the two `Ember` bottle variants `inactive`, and `SOL-TRAIL-BTL-EMBER-750ML` on one unit so an oversell is a real case). Descriptions are abridged. `createDemoStore()` builds a real `StandaloneCatalog`, `Cart`, and `OrderBook` from it, so the pages drive the same domain the CLI smoke does rather than a parallel model, and `test/demo-catalog.test.ts` diffs the fixture against the committed seed so a seed change that is not mirrored here fails the suite. It reads no database.
- `src/commerce.ts` gained additive, optional display fields on `Product` (`brand`, `category`, `color`, `size`, `description`, `image_url`, `image_alt`) so a catalogue entry can carry what a page needs to render. Every field is optional, the frozen-record allowlist was extended to match, and no existing caller or test changed behaviour: this is additive, not a contract change, and the domain still ignores presentation data.
- `src/web-server.ts` is the first browser surface for both products, on `node:http` only, with no framework and no build step. `npm run web` serves the storefront at `127.0.0.1:4312` (override `ATLAS_ECOM_WEB_PORT`) and the operator `ecom-manager` at `/manager`. The storefront renders one card per product with real colour and size selects, integer-cent prices, variant images, an availability line, and a shared cart; checking out posts to the API and shows the resulting in-memory order. The manager renders the full variant stock table, the cart, the order summary, and an Add Product form, and states the demo limits on the page itself. All pages and every JSON response label the data `demo` / `in_memory`.
- The web API is `GET /api/health`, `GET /api/catalog` and `POST /api/catalog`, `GET /api/cart`, `POST /api/cart`, `GET /api/orders`, and `POST /api/checkout`. Health and catalog report the counts a reader needs (`products`, `variants`, `sellable_variants`, `unavailable_variants`) plus `mode: "demo"`, `in_memory: true`, `database: "none"`, and `peer_connected: false`, so fixture state cannot be mistaken for a live system. A domain rejection on add-to-cart or checkout is the caller's fault and answers `400` with the domain's own message; an unknown route is `404` and a wrong method is `405`; a body over 8 KiB is `413` and is drained rather than buffered. `POST /api/checkout` returns `201` with `paid: false` and `persisted: false` on the payload, because it is neither. The `POST /api/catalog` write added by this slice is described below.
- Both pages are tokenized against `contract.md` v1.1.1. The stylesheet declares the whole shared token table once per mode as `--atlas-*` custom properties under `:root, [data-theme="light"]` and `[data-theme="dark"]`, and every rule below those two blocks resolves to a variable, so no colour literal survives outside the token table. `<html>` carries `data-theme`, the mode is normalised to `light` or `dark` with a light fallback for an absent, empty, or unrecognised value, and `?theme=` is read on the page routes only: the JSON API is untouched by it, so a `?theme=` query can never change an API response. `themedPath` keeps the page's own parameters while setting the mode, so the theme link, the cross-page link to the other surface, the post-checkout redirect, and the manager's add-product redirect and error re-render all preserve the mode instead of resetting it, and a real order shown after checkout still renders in the chosen mode. Links keep an underline so they never rely on colour alone, `:focus-visible` draws its outline from the focus token, and the demo banner, out-of-stock, warning, success, and error states use the four `state.*` pairs. One rule carries the contrast finding rather than the palette: the contract derived `fg.muted` against `bg.canvas`, so it is a canvas colour, and `.card .meta`, `.card label` and `table .meta` repaint to `fg.default` on the surface, as do `dl.stats dt` and a disabled button. A state note is also a muted line inside a card, so the warning rule gained the compound `.meta.warn` selector to out-specify that repaint instead of losing its own pair to it. This added no dependency, no build step, and no framework: it is the same server-rendered CSS the slice already shipped.
- The web surface is a demo, and the limits are enforced rather than only documented. It binds loopback and takes no host argument in the CLI entry point, has no authentication, no accounts, no CSRF token, and no audit trail, and one shared cart serves every visitor. There is one process-local `DemoStore` per server, so stopping the process loses the cart, the orders, and anything the operator added. The store is built from the standalone branch of the contract: no peer is paired, so Add Product writes straight into the in-memory catalogue and the page says that a paired peer would make it a proposal instead. Money is integer cents throughout and never a float. Every dynamic value is HTML-escaped in one pass, the two inline storefront scripts are the only JavaScript on the page, and the operator surface is explicitly a standalone demo with no connection to Atlas ERP.
- `src/web-smoke.ts` (`npm run web:smoke`) is the one-shot check: it starts the server on an ephemeral port, reads every JSON route, fetches both pages, adds to the cart, checks out, reads the orders, and asserts an unknown route is `404`, then closes the server in a `finally`. No database and no peer are needed. The scripts `web` and `web:smoke` were added to `package.json` alongside the existing smokes, and `src/index.ts` re-exports `demo-catalog.ts` and `web-server.ts` so both are part of the package surface.
- `POST /api/catalog` is the one new API route this slice added, and it exists because the `ecom-manager` frontend has to write over JSON: it takes `{name, price_cents, quantity}` and answers `201` with the created product and the catalogue counts read back from the store, not incremented by a guess. It shares one `catalogEnvelope()` helper with `GET /api/catalog`, so the read and the write cannot drift on `mode`, `in_memory`, `source`, or the shape of the counts, and a `requireNumber()` guard refuses a price or quantity that is not a finite JSON number before the domain sees it — a string, a `null`, or a boolean would otherwise coerce, and a `null` price would have created a free product. Past that guard the domain owns the verdict, so this route and the `POST /manager` form return the same message for the same bad input, because both end in `StandaloneCatalog.addProduct`. It carries the same 8 KiB body cap the cart route uses, the same `400`/`404`/`405` contract, and a `405` that now names the pair: `/api/catalog only answers GET, POST`.
- `frontend/` is a real, separate build and not a second copy of the domain. It is Vite 8 + React 19 + TypeScript (~6.0) + Tailwind CSS v4 through `@tailwindcss/vite`, with the Vite and Vitest config in one `vite.config.ts` and jsdom tests. Both surfaces are two routes in one bundle (`/` and `/manager`), not two builds: nothing in either page reaches across the other except the shared token layer, so splitting them later is a packaging change rather than a rewrite. Its one dependency on the rest of the repository is HTTP: `server.proxy` and `preview.proxy` both forward `/api` to `ATLAS_ECOM_API_ORIGIN ?? "http://127.0.0.1:4312"`, every fetch in `src/lib/api.ts` is a same-origin relative path, and no component knows where the API runs. With the API down the pages render their error state; there is deliberately no fixture fallback, because a second copy of the demo catalogue is exactly the drift `test/demo-catalog.test.ts` exists to prevent.
- The API is parsed, not trusted. `src/lib/api.ts` validates every response with zod against a schema written from the live routes, so a route that drifts fails the parse with "returned a shape this frontend does not understand" instead of rendering `undefined` into a price. A domain rejection arrives as `400` with the domain's own message and is shown verbatim, so the same rejection reads identically in the React manager and on the server-rendered one. `src/lib/money.ts` divides and pads integer cents and never multiplies, so a displayed total can only be the exact number of cents the server sent.
- The shadcn components are generated into this repository and owned here, not consumed as a package: `badge`, `button`, `card`, `input`, `label`, `select`, `sheet`, `table`, and `tabs` in `src/components/ui`, with `components.json` set to the `radix-nova` style on Radix primitives via the `radix-ui` package, lucide icons, and the `@/` aliases. That is the contract risk about a generated copy of every shadcn component the sibling ERP product also holds separately, now realised: the shadcn CLI is a declared devDependency and the README documents the one thing that must be undone after any `shadcn add` (it appends its own `:root` / `.dark` blocks with the default palette to `src/index.css`, and those blocks must be deleted so the colour bridge stays the single source). There is exactly **one** `LOCAL EDIT` marker in the generated code, in `sheet.tsx`, replacing shadcn's black scrim with `fg.default` at 10% — and its own comment explains why the black class name is written out nowhere, since Tailwind scans source text and naming it would put the utility back in the build. `frontend/README.md` says "two local edits"; only one is marked in the source, so either the second is unmarked or the README is wrong. Worth resolving before the next `shadcn add`, because an unmarked local edit is one a re-run of the CLI silently destroys. **The provenance claim is now measured rather than asserted:** a CLI diff of `button.tsx` matches the committed component on 65 of 66 lines, the sole difference being that this component resolves `cn` from a local module while the generated shape points at the configured `@/lib/utils` alias. That difference is deliberate, documented in the README, and must not be "fixed" — the sibling ERP product is in the opposite position, with a much larger divergence caused by a missing alias mapping in its root tsconfig, so the two products' answers to the same question are genuinely different and each is right for its own tree.
- `src/styles/atlas-tokens.css` is the token bridge, and it is the only file in the frontend allowed to contain a colour value. It declares the `contract.md` v1.1.1 shared Atlas UI token table as `--atlas-*` custom properties for both modes, then the v1.2.0 shadcn-to-Atlas mapping as derived variables over that table — `--background` to `bg.canvas`, `--foreground` to `fg.default`, `--card` to `bg.surface`, `--primary` to `accent.default`, `--primary-foreground` to `onAccent.default`, `--muted-foreground` to `fg.muted` on the canvas only, `--border` to `border.divider`, `--input` to `border.control`, `--ring` to `focus.ring`, and the four `state.*` pairs with `state.success.text` on the accessible ink and `state.success.indicator` as the non-text marker — and then an `@theme inline` block so a Tailwind colour utility in a component resolves through the mapping rather than needing a literal of its own. `src/index.css` holds imports and nothing else. The token-placement corrections the server-rendered pages carry as hand-written rules are here too, applied to shadcn's markup instead: `.on-surface` repaints muted text to `fg.default` (with a `.state-note` escape so a state note keeps its own pair) and gives a control on a surface the canvas as its own background, because `border.control` is 3.73:1 on the canvas and 2.79:1 on a surface in dark mode. One detail worth recording: Tailwind's shadow utilities fall back to a literal black, and the token table has no shadow token, so `--tw-shadow-color` is re-pointed at `fg.default` at 10% to keep the last colour literal in the built CSS inside that one file.
- Light and dark on the frontend close two of the three gaps the token slice left open. The mode is one `dark` class on `<html>`, set before first paint by a small inline script in `index.html` so a reload never flashes the other mode. An explicit choice persists in `localStorage` and survives a reload and a walk between the two surfaces; with nothing stored, `prefers-color-scheme` decides and keeps deciding while the tab is open. That is the persisted choice and the media query the server-rendered pages still lack, and it is scoped to this build: `?theme=` on `node:http` is unchanged. `ThemeProvider` also tolerates a blocked `localStorage` — a browser privacy mode is not a failure to render.
- `contract.md` is at v1.3.0, status ACTIVE, dated 2026-09-26, and **it is published**: the amendment shipped in `f5fc6fa` as an 83-insertion, 7-deletion diff, and it no longer misdescribes the repository it governs. v1.3.0 adds a **legacy knowledge transfer** rule (the legacy `ecom-erp` project is the evidence base for requirements, not its authority: adopt the invariant, redesign the mechanism inside `core -> cluster -> plugin -> module`, drop the accidents, and copy no source, schema, route, component, style, or configuration and import no legacy data), an **evolution map** of candidate capabilities, the **cross-product ownership** rule, a **token-parity** acceptance requirement, and **acceptance gate 8**. The evolution map lists `browse` and `variant-media` under Catalog, `storefront-presentation` under Presentation, `lifecycle` under Orders, `idempotent-admission` under Command admission, `customer` under Customer identity, `reviews` and `wishlist`, and `import` and `outbound` under Integration. It also carries the two ordering rules that make it a real map rather than a wish list: **identity before customer scope** and **persistence before customer scope** — no customer-scoped capability is approved until identity exists *and* a restart keeps what it wrote. Nothing in that table is approved scope: no capability, boundary, or dependency was added by v1.3.0. Two wordings in this contract were wrong against the repository and are now fixed in both products: the v1.3.0 amendment-history row used to read "**Proposed** legacy knowledge transfer rule" on a row whose status is ACTIVE, and the Risks section used to say the parity check "is not implemented yet". `f5fc6fa` carries both corrections here. The Risks section still says the token-parity *risk* stays open, and that is now accurate rather than stale, because it gives its two reasons — no automated run invokes the check, and it compares the two bridges to each other rather than to the token table — and both still hold.
- `docs/LEGACY_KNOWLEDGE_TRANSFER.md` is the supporting document for that contract section — 557 lines, the largest knowledge document in either product — and it is the most complete thing v1.3.0 produced. It cites the legacy tree as *evidence* with `path:line` references so a reader can find a witness without it being a template, and it grants no scope of its own: where it and the contract disagree, the contract decides. It records **ten adopted Ecom requirements as rules, not mechanisms**: 2.1 variant identity and per-variant media, 2.2 server-paginated and server-filtered browse, 2.3 server-authoritative pricing, 2.4 idempotent command admission, 2.5 order lifecycle, 2.6 customer identity (and why reviews and wishlist depend on it), 2.7 reviews and wishlist, 2.8 merchandising and storefront presentation, 2.9 validated inbound import, and 2.10 outbound integrations. Several are worth naming because they are honest about the legacy evidence being *mixed*: 2.2 records that the rule was proved on the mobile and admin paths and **failed on the storefront path in the same repository at the same time**, so the adopted rule is the mobile behaviour and the storefront behaviour is written down as a rejected anti-pattern. 2.4 is the one requirement this repository **already holds** — the durable connected-order receipt in `src/postgres-order-store.ts` with its `reserve`/`complete`/`abort` and `request_hash` — so it is cited as precedent and the transfer widens it to local commands rather than inventing it. Every other requirement in section 2 is adopted and **unimplemented**, and the document says so in its own closing section and defers to `PROJECT_STATE.md` as the authority on what this product has actually built. It then names **twelve rejected legacy defects** so a later reader restoring parity does not reintroduce them, among them colour and size as free-text JSON, broken variant image switching, a cart identity that loses the size, an idempotency fingerprint that omits the field that differs, a storefront search that navigates into a parameter the page discards, no pagination on the main listing, client-side filtering, orphaned surfaces, placeholder content presented as real, authorization from a single session boolean, and dual UIs with three competing dark systems. Two of those are worth reading for this product specifically: 4.4 (a request fingerprint that omits `size`, so two cart lines differing only by size replay the first order) is the reason 2.4 makes the *scope* of the fingerprint part of the requirement, and 4.11 (three coexisting dark-mode systems with hardcoded hex) is the direct precedent for the token-parity requirement now in the contract. It ends with the seven-row gate template for gate 8, including the two rules that make it bite: row 7 is a claim about the diff rather than an intention, and row 5 must be able to fail.
- `scripts/check-token-parity.mjs` is the cross-repository token-parity check the contract's risk section names, and the first check in this project that reads *another* repository. The two copies — one in each repo root — are identical apart from four lines of repo-local config (`SELF` / `SIBLING` name and bridge path) and a two-line comment naming each repository's own token test, and they resolve their paths from `import.meta.url`, so `node scripts/check-token-parity.mjs` works from any working directory. It reads only two stylesheets, writes nothing, and exits `0` on parity, `1` on a parity failure, `2` when a bridge cannot be read (with the message that both repos must be checked out side by side in `../`). It compares the two bridges **to each other**, holding token *names* only and no colour values at all: 20 `--atlas-*` names x 2 modes. Eighteen of them are the 14 contract rows, because each `state.*` row is a foreground on a background and so is two variables; the other two, `--atlas-success-text` and `--atlas-success-indicator`, are the contract's one named deviation — the accessible ink in both modes, with the supplied foreground kept as the non-text indicator — and `a1bc6a4` added them because that is exactly the pair the sibling's dark badge defect lived in. It strips comments first so a colour quoted in prose is never read as a declaration, keeps duplicates rather than collapsing them, and only reads a rule body when every selector in the prelude is `:root` or `.dark`, feeding both modes for a selector list the way the cascade does. Each check requires **exactly one** declaration per mode per repo, so a missing token and a duplicate both fail rather than passing by coincidence, and a value mismatch prints both sides. That matters here because this repository's own `atlas-tokens.test.ts` already runs a stricter check on this file — hex-only, colour-function, and Tailwind-palette scan across every `.ts`/`.tsx`/`.css`/`.html` under `src/`, "no colour literal anywhere else in the frontend" — so the two checks compose rather than overlap: this one proves the *other* bridge agrees, that one proves this file stays the only place a literal can live. It is wired as the **root** `package.json` script `tokens:parity`, not in `frontend/`, because this root project is the one that already owns the cross-cutting `check` script; the sibling wires the same script from its own `frontend/package.json`. One property of that list is a real limit rather than a detail: the names are **fixed in the script, not discovered from the stylesheets**, so a token a future bridge adds is outside the check until somebody adds the name by hand, and the script will report parity while a token sits in one stylesheet and not the other. That is not hypothetical — the two `state.success` variables were outside the check for its entire first life, and the only reason anyone looked was the sibling's dark badge measuring 1.10:1. The script's own header comment still reads "The 18 contract tokens ... 14 + 4 = 18", so the comment is now stale against the list two lines below it; that is a one-line comment fix in both copies rather than a check defect, and it is recorded here instead of made in this state-only pass.
- `.gitignore` covers `node_modules/` (while keeping `package-lock.json`), `.env`/`.env.*`, local database and dump files, logs, and — added in `f5fc6fa` — `.vitest/`. The root still carries no rule for the frontend slice itself, because nothing generated landed at the root other than that one: the 34 root files that existed before the frontend are untouched, and root `package-lock.json` was not modified by the frontend, which is a self-contained project with its own manifest, its own lockfile, and its own `.gitignore` ignoring `node_modules` and `dist`. `frontend/dist/` exists on this machine because the production build was run, and it is correctly ignored rather than committed. The published tracked set is **77** files: those 34, the 41 `frontend/` files, and `docs/LEGACY_KNOWLEDGE_TRANSFER.md` plus `scripts/check-token-parity.mjs`. **The untracked-file gap this section used to name is closed:** `frontend/.vitest/json/output.json` is vitest reporter output, and `f5fc6fa` added a root `.gitignore` rule for `.vitest/` that covers it, so the tree shows no untracked files again. The file itself is still on disk; it is now ignored, which is the correct treatment for build output rather than a source change.

## Verification recorded for this slice

The numbers below are for the latest run, which adds the `frontend/` build and the `POST /api/catalog` route on top of the contrast fix. They are split into what the root API measured and what the frontend measured, because they are two projects with two manifests and two test runners. **None of them was rerun after `a2edd7d` was pushed.** That commit changed no source relative to the tree the numbers were recorded on, so they still describe the published content, but they are the numbers from that run rather than a fresh reading of `HEAD`. That still holds now that both v1.3.0 commits are published: `f5fc6fa` is a contract amendment, a knowledge document, the read-only parity script, one `package.json` line, one `.gitignore` rule, and the previous revision of this file, and `a1bc6a4` touched only the parity script — no `src/` file, no `test/` file, no frontend source file, and no test anywhere, so every number below is unaffected by them. The three checks that *are* new — token parity, the browser pass, and the shadcn provenance diff — are recorded separately, each with the honest limit of what it does and does not prove. The database-backed checks need a dedicated temporary PostgreSQL instance named by `ATLAS_ECOM_DATABASE_URL`; no such database is provisioned now, so the four `PostgresOrderCommandStore` tests skip and their numbers do not reproduce.

- Root `npm test` — 101 tests, 97 passed and the 4 `PostgresOrderCommandStore` tests skipped without a database, up from 98/94/4. The increase is entirely in `web-server` (27 to 30) and the breakdown is otherwise unchanged: 19 commerce, 9 protocol, 10 `AtlasErpClient`, 6 `ErpAuditConsumer`/`ConnectedCatalog`, 5 connected-order smoke, 2 checkout-smoke guard tests (the write flag refuses before anything is configured, and the required peer and database variables are checked before a connection is opened), 10 `ConnectedCheckout`, 4 `PostgresOrderCommandStore`, 6 `demo-catalog`, 30 `web-server`. The durable tests cover a receipt replaying after the store is closed and reopened, a pending claim surviving a reopen while `abort` releases only pending claims, repeated `ensureSchema` plus four concurrent claims of one id resolving to one winner, and a full `ConnectedCheckout` whose key still replays from the reopened row without posting a second sale. The in-memory `ConnectedCheckout` tests cover the write opt-in, the conflict on a changed payload, the conflict on an in-flight key, abort on a failed submission and on a mismatched peer total, the unknown-item and oversell rejections, and a replay into a fresh `OrderBook` restoring the order exactly once.
- The 3 new `web-server` tests cover `POST /api/catalog` and are written to be hostile about it. The create case asserts the read and the write share one envelope, that the returned counts are `{products: 7, variants: 38, sellable_variants: 36, unavailable_variants: 2}` read back from the store rather than incremented, that the created product is in the same shape `GET` uses, and that it appears in the store, in the next read, and on both rendered pages. The rejection case walks eleven bad bodies and requires each to be a `400` carrying a named reason: missing, blank, and non-string names; absent, `null`, and string prices; absent and boolean quantities. The `null` price case is the one that matters — without `requireNumber` it would coerce to zero and create a free product. Negative, float, and negative-quantity inputs are asserted to reach the *domain's* message, not the guard's, which is what proves the two write paths share validation. Zero price and zero quantity are asserted to be accepted, with a zero-stock product counted as catalogued but not sellable. It then asserts nothing that was rejected was written. The third test asserts `GET` still answers, that `PUT`/`DELETE`/`PATCH` are `405` naming both methods, and that the 8 KiB body cap applies to the write.
- The previous 27 `web-server` tests are unchanged, including the 5 contrast tests: every painted text pair meeting 4.5:1 in both modes, resolved through the token table the page actually served rather than restated hex values; every control border and focus ring meeting 3:1; the dark `fg.muted` pair clearing 4.5:1 on `bg.canvas` and missing it on `bg.surface`, with the stylesheet proven to paint the muted token from the canvas rules only; every rule that paints `bg.surface` paired with the rules that paint the text inside it in `fg.default`, including the compound `.meta.warn` that keeps a state note's own pair; and the two pairs the token table cannot reach recorded as documented shortfalls. The suite was checked against the defect as well as the fix: restoring the muted colours on those five selectors fails the muted-placement and surface-repaint tests, with the diff naming every offending selector. Also still covering: the 7 theme tests (both pages carrying the whole contract token table in light and dark, every colour resolving to a token, underlined links and a focus ring from the focus token, an absent/empty/unrecognised theme falling back to light, the theme and cross-page links carrying the mode while keeping the page's parameters including a real order id, the manager keeping the mode across the add-product redirect and the error re-render, and `?theme=` reaching the pages only while leaving the JSON API alone) and the earlier 15 on payloads, selects, script validity, the manager panels, the add form, HTML-escaping, and the 404/405/loopback/ephemeral-port behaviour.
- Root `npm run check` — passed, exit 0 across all 25 source and test files (15 in `src/`, 10 in `test/`). This covers the `node:http` half only; the frontend has its own `tsc -b` and its own manifest and is not in the root project.
- Frontend `npm test` — 43 tests across 6 files, all passing: 9 `api`, 8 `atlas-tokens`, 3 `config`, 10 `manager`, 7 `storefront`, 6 `theme`. The 9 client tests are worth naming because they test the boundary rather than the client: the catalogue payload parses and every price stays an integer; a payload the frontend does not understand is rejected instead of rendered; **a float price is rejected**, because the zod schemas use `z.number().int()` so a float can never reach a formatted total; a `400` surfaces the domain's own message rather than a generic failure, on both `POST /api/cart` and `POST /api/catalog`; the `POST` bodies are asserted field by field so the two sides stay pinned; an empty cart parses rather than failing; and a create envelope missing its product is rejected. The 8 token tests are the load-bearing ones and they work on the file the build actually compiles, not a restated copy: the whole v1.1.1 table present in both modes against values restated from the contract so drift fails in either direction; every one of the 16 required v1.2.0 shadcn mappings pointing at the right `--atlas-*` token; only six-digit lowercase hex anywhere in the stylesheet, so a lookalike character cannot slip in; **no colour literal anywhere else in the frontend** — a recursive scan of every `.ts`/`.tsx`/`.css`/`.html` under `src/` for hex literals, colour functions, and Tailwind's own palette utilities, which is stricter than a hex check and is there because the generated `Sheet` shipped a black scrim utility that a hex-only check would have passed; every painted text pair at 4.5:1 in both modes; the canvas-safe control border holding the exact measured 3.83:1/3.73:1 on the canvas and 3.32:1/2.79:1 on the surface, which pins the derivation the treatment exists for; the `.on-surface` muted repaint and `.state-note` escape hatch still present in the stylesheet; and the dark mode being driven by the one class the `index.html` script sets.
- Frontend `npm run typecheck` — `tsc -b`, clean, no diagnostics. Frontend `npm run build` — `tsc -b && vite build` succeeded, emitting `dist/index.html` 1.37 kB, `dist/assets/index-*.css` 54.87 kB (9.73 kB gzip), and `dist/assets/index-*.js` 543.99 kB (168.97 kB gzip). **The build warns that a chunk exceeds 500 kB**, because both routes are in one bundle with no code splitting; it is a warning, not a failure, and the fix is route-level `lazy()` if bundle size ever matters. Frontend `npm run lint` — oxlint exits 0 with 0 errors and 9 warnings, all in the `react(only-export-components)` fast-refresh rule on `badge.tsx`, `tabs.tsx`, and `theme.tsx` (generated shadcn files and a module that exports both a provider and its hook), plus one `react(set-state-in-effect)` on `use-remote.ts:34` where the loading state is set synchronously on the first fetch. None of the nine is an error and `rules-of-hooks` is set to `error` and did not fire.
- Root `npm run web:smoke` — passed over real HTTP on an ephemeral port, reporting 6 products, 37 variants, 35 sellable variants, one order of 17800 cents, `storefront_status: 200`, `manager_status: 200`, and `unknown_route_status: 404`. 35 sellable of 37 is the two `inactive` `Ember` bottle variants from the seed, so the page and the fixture agree. The smoke exercises the server-rendered pages, not the React build: it requests pages without a `?theme=` value, so it covers the light fallback only, and nothing in it starts a Vite server. Nothing was left listening — the smoke closes its server in a `finally`.
- **Browser pass, done.** Both routes have now been driven in a real browser against a live API in light and dark mode, which was the single biggest caveat on this slice and is now recorded below rather than outstanding. It was driven with Playwright against Chromium against both live frontends — this storefront, this `ecom-manager`, and the sibling ERP console — and **56 of 57 checks passed**. What worked: the theme toggle on every surface, the live API calls from both React routes, add-to-cart and checkout, add-product through the manager, and every ERP tab. No uncaught page error and no uncaught console error on any route, and no failed API request. Screenshots are in `C:\Users\Mventor\AppData\Local\Temp\opencode\atlas-browser-pass\`: `ecom-light.png`, `ecom-dark.png`, `ecom-manager-light.png`, `ecom-manager-dark.png`, `erp-light.png`, `erp-dark.png`, plus the two badge crops `erp-badge-light.png` and `erp-badge-dark.png`. The one failing check and the three findings are all on the **sibling** surface, not this one: the dark-mode `success` / "API ok" badge on the ERP console measured 1.10:1 contrast, the ERP catalogue table clips three columns at 1440px, and the ERP `/favicon.ico` 404s. **Nothing on this product's own routes failed, and no cosmetic finding was recorded against either ecom route.** The contrast finding has since been fixed on the sibling side, in its `9f82667`, and the cause is worth recording because it points away from this repository: the ERP bridge pointed `--success-text` at `--atlas-fg-default` in dark, while **this** repository's bridge already declared `--atlas-success-text: #2d0000` in both mode blocks, so the defect belonged to the sibling and no equivalent fix was owed here. That badge now measures 12.15:1 in both modes; the two cosmetic ERP findings are still open there. The honest limit: the pass ran against `npm run dev` on both sides, not against `npm run build` output, so the production bundle is still unobserved in a browser — and the jsdom tests remain a stubbed-`fetch` claim about shapes, not a claim that the two processes interoperate, because the pass proved the *browser* end of that and not the built artefact. The pass is evidence that the frontend runs against the real API, which nothing else in this file was; it is not evidence about the data, which is still the same in-memory fixture either way.
- **The shadcn provenance claim is now measured for this repository, and the answer is "a near-exact match".** Running the CLI against a scratch copy of `button.tsx` and diffing it against the committed `frontend/src/components/ui/button.tsx` gives **65 of 66 lines identical**, and the single differing line is the import: the committed component resolves `cn` from a **local** module while the generated shape points at the configured `@/lib/utils` alias. Everything else — the variant structure, the class composition, the `data-slot` attributes, the `radix-nova` styling — matches the CLI's own output, so the claim that these components are generated and owned here is now measured rather than asserted. **The one-line difference is deliberate and must not be "fixed".** Re-pointing the import to the alias would reverse a documented local ownership decision recorded in `frontend/README.md`, and the sibling ERP product is in the opposite situation with a much larger divergence. Two things this diff does *not* settle, both already recorded elsewhere in this file: the "two local edits" discrepancy against the single `LOCAL EDIT` marker in `sheet.tsx` (a `button.tsx` diff says nothing about `sheet.tsx`, and the unmarked-edit risk is specifically about a component the diff did not cover), and the fact that a diff of one component does not speak for the other eight. The honest limit: the scratch directory has been cleaned up, so this is the recorded result of that pass rather than a diff that reproduces on demand today.
- Root `npm run smoke` — standalone catalog -> cart -> checkout still passed; the local domain path is unchanged, and this slice touched no domain code.
- `python -B tests/cross_process_smoke.py` from the sibling `atlas-erp` repository — not rerun for the web slice, the tokenization, the contrast fix, or the frontend, because it needs the peer database that no longer exists. The last result, from the published commit, was: passed, two real processes over real HTTP with a real peer database. The harness runs the read smoke, then the connected checkout smoke, then the lower-level order smoke. The checkout smoke reported `durable_replay: true`, `replayed: true`, and `order_store: "postgres"`, and it exited zero only because the peer's audit cursor advanced, stock fell by exactly the sold quantity, the replay left the peer's cursor and stock untouched, and the `OrderBook` held exactly one connected order.
- `npm run tokens:parity` (`node scripts/check-token-parity.mjs`, at the root) — **exit 0**, both in this repository and in the sibling. It reports `20 contract tokens x 2 modes, one declaration each, atlas-ecom and atlas-erp identical`. The same command is wired as the sibling's `frontend/package.json` script and reports the mirror of the same line with the two names swapped, which is the check being genuinely two-sided rather than one script printing a fixed string. This is the first check in the project that reads the other repository, and it is what closes the contract's token-parity risk as an *implemented* check rather than a requirement. It composes with the existing `atlas-tokens.test.ts` rather than repeating it: that suite pins this bridge against the contract table *and* proves no colour literal exists anywhere else in `frontend/src`, while this script proves the sibling's bridge declares the same 20 names with the same values. Three limits worth stating rather than hiding. It requires both repositories checked out side by side as siblings, and exits `2` with that message if either bridge is unreadable, so it is a developer check and not yet a CI gate — nothing runs it automatically. And it compares the two bridges to each other, so a value edited in **both** files consistently would pass here; that is deliberate, because it makes the check impossible to satisfy by restating a stale copy, but it means the contract table itself is still pinned only by the in-repo suites, not by this script. The third is the one the sibling's own bug found: the name list is fixed in the script rather than discovered from the stylesheets, so a token a future bridge adds sits outside the check until somebody adds the name, and the two `state.success` variables sat outside it until `a1bc6a4`.
- **The parity check was measured against mutations, not just run once.** Passing on the current tree proves nothing about a check that cannot fail, so it was run against four deliberate breakages: a token **removed** from one bridge, a token **declared twice** in one mode block, a token **given a different value** in one bridge, and — after the list was widened — the dark `--atlas-success-indicator` set to `#2a7c13`, tried on this repository's bridge and on the sibling's. All four were caught and reported per-token and per-mode with both sides printed, the first two as a count other than one and the rest as two differing values, and each exited `1` rather than `0`; the `#2a7c13` value was restored afterwards. The missing-token and duplicate cases are the ones a naive "read the first match" implementation would have passed, so they are the load-bearing mutations here, and the fourth is the one that proves the two names `a1bc6a4` added are genuinely compared rather than merely present in the list — a name that was added but never reached would have passed silently.
- `git status --short --branch` — **the tree is clean apart from this file, and the previous revision of this file said the whole v1.3.0 follow-up was uncommitted.** `## main...origin/main` with no ahead/behind count, and `HEAD` resolves to `a1bc6a4`, which is also where `origin/main` resolves. The only working-tree entry is ` M PROJECT_STATE.md`, which is this revision and nothing else: `docs/`, `scripts/`, the `tokens:parity` line in `package.json`, the `.gitignore` `.vitest/` rule, and the v1.3.0 `contract.md` are all committed and pushed, so the earlier "`git status --porcelain --untracked-files=all` is empty" describes the tree again and the intermediate revision's five uncommitted entries are out of date. The published set is **77** tracked files: the 34 root files, the 41 `frontend/` files with `frontend/package-lock.json`, `frontend/components.json`, and `frontend/README.md` still in `a2edd7d`, plus `docs/LEGACY_KNOWLEDGE_TRANSFER.md` and `scripts/check-token-parity.mjs` from `f5fc6fa`. The earlier content-level check stands as measured in the v1.2.0 era — all tracked text files at `HEAD` had 0 trailing-whitespace lines, 0 hard CRs, 0 tabs, and a final newline — and `git diff --check a2edd7d..HEAD` across the two v1.3.0 commits is clean, so the published v1.3.0 content adds no trailing whitespace and no conflict markers. This revision of this file sits outside both numbers, because it is not committed.
- `git diff --check` — no whitespace errors, exit 0. Its honest limit is back where it was before the v1.3.0 work: with a tree clean apart from this file it has almost nothing to inspect, so it is close to a vacuous pass and says nothing about the committed content at `HEAD`. The wider form is the useful one — `git diff --check a2edd7d..HEAD` over `f5fc6fa` and `a1bc6a4` is also clean, which puts the published v1.3.0 content inside the check rather than outside it. The content-level check over the committed tree stands as before.
- `contract.md` **was** modified by this slice and `connect/SPEC.md` was not. The v1.2.0 amendment is committed and pushed as `a954e3d`, and the code implementing it is committed and pushed as `a2edd7d`; between them they changed no contract or spec text. The v1.3.0 amendment on top of it is an 83-insertion, 7-deletion `contract.md` diff that is **committed and pushed as part of `f5fc6fa`**: version, status, date, and approval date to v1.3.0 / ACTIVE / 2026-09-26, then the legacy knowledge transfer section, the evolution map with its two ordering rules, the token-parity requirement in the Token mapping section, the cross-product ownership subsection, acceptance gate 8, and the amendment-history row. The previous revision of this file recorded that diff as 78 insertions and 4 deletions, and the difference is the two wording corrections the same commit carried: the amendment row's "Proposed" became "Approved", and the Risks section stopped saying the parity check "is not implemented yet" and now names the script. It changes no spec text, no `src/` file, no `test/` file, and no frontend source file. One contract question from the contrast work is still open and still recorded rather than answered: both shortfalls there are token-table values rather than missing rules, so closing either is a contract decision. The frontend bridge takes `state.success.text` as `#2D0000` and keeps the supplied `#2A7C13` as `state.success.indicator`, which is the contract's own suggested resolution rather than a new decision, and `atlas-tokens.test.ts` pins both. That pair is now also inside the cross-repository parity list, and it is the pair the sibling got wrong in dark mode.


## Verification recorded for the example database

No code reads `db/`, so the seed was checked by loading and querying it in a dedicated temporary PostgreSQL container named `atlas-seed-check`, together with the `atlas-erp` example seed, and running the acceptance queries documented in each `db/README.md`.

- Ecom row counts: 6 categories, 6 products, 37 variants, 37 variant images, 37 stock rows. ERP in the same container returned 6 items, 37 variants, 37 variant images, 51 stock rows, 2 warehouses, 2 suppliers, 2 purchase orders and 10 purchase-order lines.
- Invariants all came back as required: zero variants without an image, and zero image rows whose `image_url` is not an `images.unsplash.com` link, whose `source_url` is not `unsplash.com`, or whose alt text is blank. The seed shares one photo per colour, which `db/README.md` documents as 13 distinct photos across the 37 images; that distinct count was not separately re-queried in this slice.
- Cross-repository identity: the `product_id` set and the `variant_id` set in the Ecom seed are identical to the `item_id` and `variant_id` sets in the `atlas-erp` seed, so the two example catalogues join by id rather than by name or price.
- Idempotence: re-running `db/schema.sql` then `db/seed.sql` in both databases changed no row count and left the same identity sets.
- The image URLs were not re-checked for reachability in this slice, and no licensing or attribution review was performed. That review is deferred below, not done.
- The `atlas-seed-check` container and its databases were removed afterwards, so these counts no longer reproduce until a scratch instance is provisioned again.

## Repository publication

The repository is published rather than local-only. The foundation commit, `4f7936c5176b69dc419184ba8477f50965f21863` ("feat: establish Atlas Ecom foundation and example catalog"), is on `main` and is pushed to `origin` at `https://github.com/mventor-git/atlas-ecom.git`; GitHub's default branch is `main`, and the repository is non-empty. Publication has continued since: the web UI as `9f5b453`, the v1.1.0 and v1.1.1 contract amendments as `bca8564` and `1e86377`, the tokenization as `e94a6a0`, the contrast fix as `0565479`, the v1.2.0 shadcn direction as `a954e3d`, the frontend implementation as `a2edd7d`, the state file as `3bd58d7`, the v1.3.0 unit as `f5fc6fa`, and the parity-list fix as `a1bc6a4`. **There is no unpublished exception left.** `origin/main` and `HEAD` both resolve to `a1bc6a4` with no ahead/behind count, and every claim in this file — the v1.2.0 React build, the legacy transfer, the parity check — is reviewable in a published commit. The v1.3.0 unit went out as one commit, `f5fc6fa`, carrying the contract amendment, `docs/LEGACY_KNOWLEDGE_TRANSFER.md`, `scripts/check-token-parity.mjs`, the `tokens:parity` wiring in the root `package.json`, the `.gitignore` rule, and the then-current revision of this file; `a1bc6a4` widened the parity list on its own after that, because the two names it added were a gap the sibling's badge finding exposed rather than something the first pass could have known. The only thing not in version control now is this file's own current revision, which is what a working tree is. Four earlier statements here claimed an unpublished set — the tokenization, then the contrast fix, then the frontend, then the v1.3.0 follow-up — and all four are corrected above; none of them was a code defect, only a documentation lag, and the fourth is the lag this revision exists to close. The publication gap the earlier revision of this section named is closed for a reviewer: a fresh clone of `main` installs both the Node API and the React frontend. `package-lock.json` is published at the root and `frontend/package-lock.json` is now published too, so both dependency trees are pinned and `npm ci` is the right command in both places. Neither `node_modules` is published, and `frontend/dist/` is gitignored, so the production build has to be re-run to be reproduced. Publication changed no technical fact recorded in this file: the deferred items below are still deferred, both routes have now been driven in a browser with the limits recorded above, and the seed verification still needs a scratch database to reproduce because no Atlas test container or database was left running.

## Cleanup result

The temporary dedicated container that held the `atlas_erp` and `atlas_ecom` test databases was removed after the run: both databases were dropped, the container was removed, and the leftover ERP connect-server process was stopped. Read-only confirmation afterwards: no `atlas_erp`/`atlas_ecom` container exists, no Python process is running the ERP server, and nothing is listening on the ERP connect-port range. The `atlas-postgres` container, which holds only `atlas_hq`, and the legacy `ecom-erp` container were both left untouched and running. `node_modules` was removed after testing while `package-lock.json` was kept, so the dependency tree is pinned but not installed; that has since changed, because `node_modules` is present again and every check in this section ran against it without an `npm ci`. The remaining practical consequence: `ATLAS_ECOM_DATABASE_URL` no longer names a live database, so the four durable order-store tests skip until a test database is provisioned again, and a fresh clone still needs `npm ci` before anything can run.

A second temporary container, `atlas-seed-check`, was created only to load and verify the two example seeds, and was removed together with its databases once the checks were recorded. Read-only confirmation afterwards: no such container exists. The versioned `db/*.sql` files are example SQL, not a live database, so nothing needs to stay running for them to be readable.

The web slice left no server behind either, and neither did the tokenization. `test/web-server.test.ts` starts the server on an ephemeral port and closes it in each test, and `npm run web:smoke` closes its own server in a `finally`. The tokenization needed a live server to check both modes by eye, so `npm run web` was started by hand on the default port `4312` and the sibling ERP console on `4311` for the same check, and the previous two revisions of this file reported both as still running — pids 15488 and 15412, started 2026-09-25 23:43, with the `4312` process serving the pre-fix stylesheet. **That is no longer true: both have been stopped.** Read-only confirmation after the push found nothing listening on `127.0.0.1:4312` or `127.0.0.1:4311`, and none on `5173` or `4173`. The `atlas-postgres` container holding only `atlas_hq` and the legacy `ecom-erp` container were both left untouched. **The browser pass and the v1.3.0 follow-up did leave servers running, and that was the one cleanup item outstanding. It is no longer outstanding: none of the four is listening.** The pass needed both frontends and both APIs live simultaneously, so four processes were up: `npm run web` on `127.0.0.1:4312` (pid 19864), this frontend's Vite dev server on `5174` (pid 23624), the sibling ERP console on `4311` (pid 22828), and the sibling ERP frontend's Vite dev server on `5173` (pid 15460). A read-only listen check finds nothing on any of those four now, and nothing on `4173` or `4310` either. They were process-local demo servers over the in-memory `DemoStore` and the ERP fixture, so nothing durable was ever at risk — stopping the ecom server discards the cart, the orders, and anything the operator added during the pass, which is the same thing the demo promises on its face — and stopping them was left to the parent rather than done from a state pass, which is why the previous revision reported them as still up. The Playwright harness itself lives outside the repository in `C:\Users\Mventor\AppData\Local\Temp\opencode\pw-harness\` and its screenshots in `...\atlas-browser-pass\`; neither is inside either project, and nothing about the pass wrote into `frontend/`. The frontend's own `npm run dev` and `npm run preview` are the commands that bind ports, and the dev server was started for the pass; `preview` never was, and `4310` (the ERP Connect API) and `4173` are free. The `atlas-postgres` container holding only `atlas_hq` and the legacy `ecom-erp` container were both left untouched. Three things on this machine are build output rather than leftovers: `frontend/dist/` (gitignored, from `npm run build`), `frontend/node_modules/`, and `frontend/.vitest/json/output.json` — the last of which **is** gitignored now, because `f5fc6fa` added the root `.vitest/` rule, so it no longer shows up in `git status`. The practical consequences are otherwise unchanged: `npm ci` must run at the root before `npm test`, `npm run check`, `npm run web:smoke`, or any smoke, a frontend run needs an install inside `frontend/` as well, and `ATLAS_ECOM_DATABASE_URL` still names no live database, so the four durable order-store tests skip until a test database is provisioned again.

## Explicitly deferred

- What the token slice did *not* finish. `contract.md` reached v1.1.1 with that
  slice, and v1.1.1 replaced the short palette section with the full shared Atlas UI token table (`bg.canvas`, `bg.surface`, `fg.default`, `fg.muted`, `accent.default`, `link.default`, `border.divider`, `border.control`, `onAccent.default`, `focus.ring`, and the four `state.*` pairs) in light and dark, records the three derived contrast-safe values and the light `state.success` 3.38:1 caveat, and names shadcn/ui as the preferred but replaceable component foundation. The storefront and the `ecom-manager` now apply that whole table as named tokens and are checked in both modes on both pages, so the design-token gate is demonstrated on these surfaces. Three parts of it are still open **on those two `node:http` pages**: the mode there is a `?theme=` parameter rather than an operating-system preference, so a first-time visitor always gets light and nothing persists a choice; there is no `prefers-color-scheme` media query; and shadcn/ui is not adopted *there*. That last one was deliberate at the time — these pages have no build step and shadcn is a build-step React component library, so the contract's "preferred but replaceable" wording was satisfied by using the tokens directly. It is no longer the whole picture: the React build in `frontend/` does adopt shadcn on Radix, and it closes the other two as well, so the gap is now scoped to the server-rendered pages rather than to the product. This bullet still describes the pages, not the frontend. The gate is not met as written on these surfaces, and this file does not claim otherwise: every named token resolves on both pages in both modes and the muted-on-surface defect is closed, but two painted pairs are still short of AA, so what is demonstrated there is the token layer only, and everything underneath both pages is still fixture and in-memory data. The contrast fix recorded the rest of it: the muted-on-surface defect is closed, and what the suite could not close is written down as `DOCUMENTED_SHORTFALLS` rather than papered over. The light `state.success` pair measures 3.38:1 and the contract itself calls it non-text and large-text only, yet `.ok-note` paints it as a .9rem status line after a checkout and after an add, so light mode is short of AA text there; the contract's own suggested repair is `#2D0000` on `#C7D3C0`, and no token in the table is that pair in both modes (`fg.default` is `#DFD8C8` in dark and `onAccent.default` is `#41444B`), so this is a contract decision, not a rule the stylesheet may take. Dark `border.control` on `bg.surface` is 2.79:1 for the same reason: the contract derived it against `bg.canvas`, where it is 3.73:1, and every control is painted on the surface. The one-line stylesheet answer is to fill controls with `bg.canvas` instead of `bg.surface`, which is what the ERP console does for its neutral chips; that was not applied here because the delegated scope for this slice named the text colours to change, so it is left for the parent to take or drop. The contrast assertions are computed from the token table rather than from a browser, so the cascade is pinned by selector text and the specificity claim in the comment above `.warn`   has not been re-checked by eye.
- Acceptance gate 7 is demonstrated on the demo surface, and this file will not claim more than that. What is now true: the approved stack exists and builds, the storefront and the `ecom-manager` are React 19 components on Tailwind v4 with nine shadcn components on Radix primitives, the fixed v1.2.0 shadcn-to-Atlas mapping is declared once as derived variables over the v1.1.1 token table and pinned by a test that fails if any of the 16 required mappings moves, the mode is a persisted choice with a `prefers-color-scheme` fallback, and `vite build` emits a working production bundle. What is still not true, and keeps gate 7 from being met outright: the surfaces are a demo. There is no authentication, no accounts, no sessions, one shared cart, no CSRF token, no audit trail, no persistence, no payment, and no ERP connection, so an order created through the React checkout is an in-memory `Order` that a restart erases and a checkout that the API itself labels `paid: false, persisted: false`. Everything underneath both routes is still the `db/seed.sql` example catalogue served by a process-local `DemoStore`. The UI also still renders a *second* view of stock and does not consume `ConnectedCatalog` at all, so an operator on the React manager is not looking at the peer's stock — the same gap the server-rendered manager has. The contract's own gate wording should be read against that, and this file records the gap rather than resolving it.
- **The end-to-end caveat on this slice is now closed, and closing it changed nothing about the verdict.** The previous revision of this file said gate 7 had not been demonstrated end to end because no browser was ever driven. Both routes have now been driven in Chromium against the live API in both modes, and everything the pass was supposed to check worked: the theme toggle, the API calls, add-to-cart, checkout, add-product, and the mode surviving across routes. So the frontend is no longer an unobserved build, and the 43 jsdom tests are no longer the *only* evidence that it does anything. Three limits keep the rest of the paragraph true rather than obsolete. The pass ran against the **dev server**, so `npm run build` output is still unobserved in a browser, and the built output has no equivalent of the dev proxy, so serving it needs a static server plus the API behind the same origin — the same open deployment question the sibling records. `config.test.ts` still asserts the `/api` proxy by reading `vite.config.ts` rather than by starting a server; the pass exercised the working proxy, so the assertion is now backed by an observation, but the test itself is still a configuration read. And the pass says nothing about durability: checkout worked, and the order it produced is the same in-memory `Order` a restart erases.
- **Acceptance gate 8, the legacy transfer gate, is not met by anything in this repository** — and it is not close, which is the honest reading of what v1.3.0 added. The gate requires that a capability whose requirement was adopted from legacy knowledge ship only with a public seam, a stated invariant, a named owning cluster, explicit failure semantics, a real test that runs, and defined loading/empty/error/success UI states. `docs/LEGACY_KNOWLEDGE_TRANSFER.md` supplies the seven-row template and the ten adopted requirements, but **no capability has been promoted from the evolution map, so nothing here has been through the gate.** The nearest adjacent work is one row, and the document says so itself: requirement 2.4, idempotent command admission, is the single adopted requirement this repository already holds, for the connected order path only, and it is cited as *precedent* for widening the rule to local commands rather than as gate-8 evidence — the receipt is a Connect command path, not a capability whose requirement was adopted from the legacy tree, and the gate explicitly refuses an absent, skipped, or stubbed test as evidence, which is also what the four currently-skipped `PostgresOrderCommandStore` tests would be. The two ordering rules make the map a sequence rather than a list: `reviews` and `wishlist` are not approvable until `customer` exists, and no customer-scoped capability is approvable until a restart keeps what it wrote — and this repository currently owns exactly one table, the connected-order receipt, with `OrderBook` and the whole local commerce path still in memory. That ordering is the same durability work the rest of this file already argues for, which is convenient rather than coincidental.
- **The token-parity risk, and the two contract wordings that misdescribed this repository, are now resolved; the previous revision of this file listed all three as open.** `contract.md` v1.3.0 added the parity requirement to the Token mapping section and, in the same amendment, left the Risks section saying the check "is not implemented yet, so this risk stays open" and the amendment-history row reading "Proposed legacy knowledge transfer rule" on a row marked ACTIVE. All three of those were wrong against the repository as it stood, and `f5fc6fa` corrected all three here and in the sibling. The Risks section now names `scripts/check-token-parity.mjs`, and the risk it keeps open is a different and still-true one: no automated run invokes the check, and it compares the two bridges to each other rather than to the token table. What no wording fixes is the check itself — it is a developer command, not a gate — and that limit is recorded in the bullet below rather than solved here.
- The two server-rendered `node:http` pages are now redundant with the React routes and nothing reconciles them. Both still serve and the 30 `web-server` tests still pass, and the manager's `POST /manager` form and the new `POST /api/catalog` are the same write with two different answers. Nothing chooses between them, no route redirects, and there is no deprecation notice on either. That is two real UIs for one demo, which is more surface to keep honest than the contract's "separate frontends" wording intended, and reconciling them is a product decision rather than a mechanical one: delete the old pages and their 30 tests, keep them as a no-React fallback, or keep both deliberately. Until that is decided, the honest position is that the token work, the theme work, and the accessibility work all now exist twice, in two different frameworks, with only the React copy getting `prefers-color-scheme` and a persisted mode.
- The frontend's two contracts with the contract itself are now **half** enforced across repositories, and the half that was missing is closed. `atlas-tokens.test.ts` pins the v1.1.1 table and the v1.2.0 mapping as *restated* values inside this repository, which catches drift in one direction only — someone editing the contract, or editing the sibling ERP product's mapping, does not fail this suite. `scripts/check-token-parity.mjs` is the missing direction: it reads the sibling's bridge and fails if the two disagree, and it was proven to fail on a missing token, a duplicate declaration, a mismatched value, and — after the list was widened — the dark `--atlas-success-indicator` on either side. So the sibling's mapping can no longer move alone without a failing check. What is still open, and this file states it rather than declaring the risk closed: the script needs **both repositories checked out side by side**, so it cannot be a unit test in either project and nothing runs it automatically; it compares the two bridges to each other, so the contract table itself is still pinned only by the two in-repo suites and someone editing the contract in both bridges at once would pass everything; and its list of names is fixed in the script rather than discovered from the stylesheets, so a token added to one bridge and not the other is invisible until somebody adds the name. That third limit is not theoretical — the two `state.success` variables were outside the list while the sibling's dark badge was wrong, and the badge measurement is the only reason the omission was found. The shadcn risk is realised too and is now *measured*: a generated copy of every shadcn component the sibling ERP product also holds separately exists here in `frontend/src/components/ui`, and a CLI diff shows 65 of 66 lines of `button.tsx` matching with the one difference being a deliberate local import decision, so the "generated and owned here" claim holds. Re-running the CLI will still re-append shadcn's default palette to `src/index.css` unless the documented deletion step is followed; `atlas-tokens.test.ts` catches that — a colour literal anywhere but `atlas-tokens.css` fails the suite — but only once someone runs it, and the shadcn risk that remains is drift between the two products' copies with no shared package to upgrade once.
- The bundle is one chunk and is over the build's own warning threshold. `vite build` emitted 543.99 kB of JavaScript (168.97 kB gzip) in a single chunk and warned about it, because both routes are bundled together with no code splitting. It is a warning rather than a failure and the fix is route-level `lazy()`, but it is recorded because a demo that ships its whole UI in one request is a demo, not a product. `frontend/` also has 9 open oxlint warnings, all fast-refresh or set-state-in-effect advisories on generated shadcn files and on `theme.tsx` and `use-remote.ts`; zero errors, and `react/rules-of-hooks` is set to `error` and did not fire.
- `OrderBook` is still in memory. A connected order is durable only as a receipt row in `connected_orders`; after a restart the book is empty and the order reappears only when the same key is replayed through `ConnectedCheckout`. Nothing scans the table at startup to rebuild it, so `summary()` and `listOrders()` do not reflect prior orders until something replays them.
- The local commerce path is still entirely in memory. `StandaloneCatalog` products, `Cart` contents, and the order returned by `Cart.checkout()` are lost on restart. Only the connected order command is durable, so local and connected orders now have genuinely different durability and there is no single order store behind both.
- The dual-write window between Ecom and the peer is open. `reserve`, the peer `POST`, and `complete` are three separate operations with no transaction spanning them, so a crash after the peer accepted the sale but before the receipt was completed leaves a `pending` claim that reports "submission in progress" with no automated recovery. The derived `ecom-${key}` sale id is what keeps a manual recovery from becoming a second peer sale, but recovery itself is currently a manual `abort`. Closing this needs a lease or expiry plus an explicit reconcile step, added when a real crash is in scope.
- `PostgresOrderCommandStore` uses a single `pg` pool and no statement timeout, retry, or health check; a database outage surfaces as a rejected promise from the checkout. Operational database behaviour belongs with the real persistence work.
- The customer storefront and the `ecom-manager` UI exist now in two copies, as in-memory demos only. There is a server-rendered pair on `node:http` and a React pair in `frontend/`, and neither is a product surface: no authentication, no accounts or sessions, one shared cart for every visitor, no CSRF protection, no audit trail, no persistence, and no connection to a peer. All four pages say so on their face. A submitted or checked-out order is still neither paid nor persisted, so payments, order persistence, and real customer accounts remain deferred along with them. The manager's Add Product is the standalone branch written straight into the in-memory catalogue on both copies, and it becomes a Connect proposal only once a peer actually holds the capability. Doubling the surface doubled the place where that demo limit has to be stated.
- The React frontend requires a running API and has no fallback. `npm run dev` and `npm run preview` proxy `/api` to `ATLAS_ECOM_API_ORIGIN ?? "http://127.0.0.1:4312"`, so two processes are needed to see either route, and without the API the pages render their error state. That is deliberate — a second copy of the demo catalogue inside the frontend is exactly the drift `test/demo-catalog.test.ts` exists to prevent — but it does mean the frontend cannot be opened as a standalone artefact, and there is no static fallback for a demo someone wants to click without a terminal.
- Full HTTP pairing/transport, one-time-code and scoped-credential exchange, TLS, hardened authentication, production security, and real degradation/resynchronization. The current loopback exchange is a narrow profile.
- Connected business operations beyond the single connected order command, presentation consumption, connected Add Product proposals, catalog/order synchronization back into Ecom, authority/import behavior, and full Connect conformance.
- Cart limitations are deliberate and unchanged: it uses the catalog price at the last add, and product quantity is availability only until checkout. `ConnectedCatalog` is a separate read projection and does not feed `Cart`, so a connected checkout is not a cart checkout.
- The real Ecom persistence schema. `db/` is example data in its own `ecom_example` schema with no migration runner, no version table, and no down-migration, and no code reads it. The contract's Ecom tables still have to be built and the local catalog, cart, and orders pointed at them.
- The web UI renders a *second* view of stock, and now it does so twice. The storefront's availability is the `StandaloneCatalog` quantity in the demo fixture, and the manager's stock table is the same quantity on both the server-rendered and the React manager, while the connected path reads stock from ERP through `ConnectedCatalog`. Neither manager consumes the connected projection at all, so an operator looking at either is not looking at the peer's stock. That gap closes when a manager reads `ConnectedCatalog` and the fixture stops standing in for real data, not before.
- Licensing and attribution for the example imagery. The 37 image rows point at 13 shared `images.unsplash.com` CDN links as placeholders, no image files are committed, and this repository carries no photographer attribution. Linking a CDN URL is not the same as being licensed to sell the photograph, so before anything reaches a real storefront each photo needs the normal Unsplash licence review at the time of use, per-photographer attribution where the photographer has asked for it, the API terms if the links ever switch to the API, and a decision on hotlinking and caching versus downloading and self-hosting. The demo pages render those same links and carry the warning in their footers, which is a placeholder, not a review. That review is product work and has not been done.

## Next safe slice

**The v1.2.0 frontend is built and published, and the browser pass it was resting on has now been run.** `a954e3d` is the v1.2.0 direction, `a2edd7d` is its implementation, and the pass drove both routes in Chromium against the live API in both modes with nothing failing on this product's surfaces. What that changes is narrow and worth being precise about: the *unverified* claim is gone, and the *demo* claim is exactly where it was.

The immediate next thing is not code. **The v1.3.0 follow-up is published, so the two housekeeping items this section used to lead with are done**: `f5fc6fa` committed the contract amendment, `docs/LEGACY_KNOWLEDGE_TRANSFER.md`, `scripts/check-token-parity.mjs`, and the `tokens:parity` wiring as one reviewable unit, and in the same commit corrected the two contract wordings that made the contract misdescribe its own repository; `a1bc6a4` then widened the parity list to the two `state.success` variables. The `.vitest/` ignore rule rode along in that first commit, so `frontend/.vitest/json/output.json` is no longer the odd thing in the tree that is neither source nor intentional. **What is left in this corner is one line in a README and one stale comment in a script.** The `frontend/README.md` "two local edits" discrepancy against the single `LOCAL EDIT` marker in `sheet.tsx` stays open on purpose: an unmarked local edit is one a re-run of the shadcn CLI silently destroys, a `button.tsx` diff does not settle it, and only somebody who remembers making the second edit can say whether the README is wrong or the marker is missing. And the parity script's own header comment still reads "The 18 contract tokens ... 14 + 4 = 18" in both copies while the list two lines below it holds 20 — a one-line comment fix that should land with the next edit to either script, in both, since the copies are meant to stay identical.

**Both items the previous revision of this section listed under "ride along with that pass" are now done**, so they are recorded rather than repeated. The `shadcn add` diff ran: 65 of 66 lines of `button.tsx` match the generated shape, and the single difference is a deliberate local import decision recorded in `frontend/README.md` that must not be reverted to the alias. That is the good outcome for this repository and it is not the sibling's: the ERP product's own diff came back at 35 insertions against 23 deletions because its root `tsconfig.json` is solution-style and carries no `compilerOptions.paths`, so the CLI there resolves `@/` to a literal path, writes to the wrong place, and bumps `radix-ui`. **The sibling is where the follow-up belonged, and it has now been taken there**: the ERP product's fix pass is published in its history as `9f82667`, the sibling repository is at `da38b63`, and the dark `state.success` badge that measured 1.10:1 now measures 12.15:1 in both modes. The cause was on that side, not this one — the ERP bridge pointed `--success-text` at `--atlas-fg-default`, while this repository's bridge already declared `--atlas-success-text: #2d0000` in both mode blocks — so nothing was owed here, and that fix is what exposed the two names the parity list had been missing. What that pass did about the `tsconfig` alias divergence described just above is not recorded here, so nothing in this file should be read as claiming that one is closed. A one-line `frontend/README.md` correction against the single marked `LOCAL EDIT` is the only outstanding item in this repository from the shadcn provenance work. Two limits on the good news, so it is not over-read: the diff covered `button.tsx` only, so it says nothing about the other eight generated components, and the one-line import difference is exactly the kind of local ownership decision that a future `shadcn add` will try to undo.

The one verification the pass did *not* complete is the other half of the same question: it ran against `npm run dev`, so `npm run build` output has still never been opened in a browser. That is the artefact a deployment serves, and unlike the dev server it has no `/api` proxy, so it cannot simply be pointed at the running API — it needs a static server plus the API behind the same origin, which is the deployment decision the sibling records and the reason this stays open rather than being a five-minute repeat.

Decide what to do with the two server-rendered pages, because leaving them is a third option nobody chose on purpose. They still serve, the 30 `web-server` tests still pass, and they now duplicate a React pair in a different framework with a worse theme story. Reconciling is a product call: delete them and their tests, keep them as a no-React fallback, or keep both deliberately. This file does not assume an answer.

The durability work below is unaffected and still outranks any further surface. Both managers show a cart, an order count, and a stock table that a restart erases, so the next slice that touches the existing domain code should make the *durable* fact visible rather than add a third surface — and another UI is not the way to do it. Make the durable receipts visible without a replay, then close the dual-write window. Concretely, add a read-only `listConnectedOrderKeys` (or equivalent) to `OrderCommandStore` and a `ConnectedCheckout.restore` that rebuilds a fresh `OrderBook` from every completed row in `connected_orders`, so a restart recovers its connected orders without a caller replaying each key by hand. That is a small addition to the interface Ecom already owns, needs no peer call, and is directly testable against a temporary database once one is provisioned again. Only after that, add a lease or expiry to the pending claim plus an explicit reconcile of "peer accepted, receipt never completed" against the peer's own idempotency. Keep standalone catalog, cart, and local checkout unchanged, and keep the peer write behind the explicit `ATLAS_ERP_ALLOW_WRITE=1` guard. Defer the full local persistence, the authenticated product versions of all four pages, payments, TLS, full pairing, presentation, connected Add Product proposals, and durable outbox until then. Treat `db/` as loadable example data that must be kept in step with the `atlas-erp` seed in the same commit whenever the shared catalogue identity changes, and re-verify the seeds after any such change because the `atlas-seed-check` container that verified them is gone.

On the manager itself, the remaining work follows from that restore step: back the orders panel with the `connected_orders` receipts it recovers, so a restart shows the orders that actually survived instead of an empty book. Only after that does the storefront need anything new; a customer-facing page over state that vanishes on exit is a worse demo than an honest one, and all four pages already say so. Keep `src/demo-catalog.ts` in step with `db/seed.sql` in the same commit, because `test/demo-catalog.test.ts` fails on drift, and keep the fixture clearly labelled as the stand-in it is until the contract's Ecom tables exist. The frontend adds two obligations of its own, both already enforced and both worth keeping: `atlas-tokens.test.ts` fails if a future edit puts a colour literal, a colour function, or a Tailwind palette utility anywhere outside `atlas-tokens.css`, which is the check that catches a re-run of the shadcn CLI re-appending its default palette; and `api.test.ts` fails if a route's shape drifts from its zod schema, so a change to the `node:http` JSON contract cannot silently become `undefined` in a price. The ERP console was tokenized in parallel and is a separate loopback process on its own port; the UIs are deliberately not integrated, and wiring them together is a contract decision rather than a UI task.
